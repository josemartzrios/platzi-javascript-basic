const numbers = [5, 30, 60, 120];



// MAP: devuelve un nuevo array transformado (máquina transformadora);
const dobles = numbers.map(function(numero){
    return numero * 2;
});

console.log(dobles);
